# Runtime dependencies (pinned for reproducible builds — macOS & Windows).
streamlit==1.52.2
pyatem==0.10.0
pyusb==1.3.1
rich==14.2.0
ifaddr==0.2.0      # network interface enumeration (discovery)
zeroconf==0.148.0  # mDNS ATEM discovery
